[b][size=5]Rose Winters (RE8) - Argon Female NPC Replacer[/size][/b]

把《生化危机8：萝丝之影》的成年萝丝做成《X4：基石》里的 Argon 女性 NPC 外观。

采用 X4 标准的「换网格、留骨架」方式：使用原版共享骨架与全套共享动画，
不修改任何游戏原始文件。

[b]本 mod 有两种形态，两个下载文件，文件名即配置 —— 二选一，不要都装[/b]：

[list]
[*][b]x4_rose_argon_add_v1.3.zip[/b] —— [b]加入外观池[/b]（正常游玩用这个）：萝丝成为
Argon 女性外观池里的[b]一个随机候选[/b]，约占该职业女性的 1/4（平民池 6 个候选，约占 1/7）。
原版模型一个都没动，其余女性保持自己的脸、名字与语音。
[b]剧情/任务 NPC 不经过外观池，因此保持原版外观。[/b]
[*][b]x4_rose_argon_replace_v1.2.zip[/b] —— [b]全部替换[/b]（测试 / 想满地图都是萝丝时用）：
所有 Argon 女性（含剧情/任务 NPC）都是萝丝；它会覆盖其它 Argon 外观替换 mod。
[/list]

[b]版本[/b]：add 1.3 / replace 1.2　　[b]游戏版本[/b]：X4: Foundations 9.00

[line]

[b][size=4]功能[/size][/b]

[list]
[*]用萝丝·温特斯的模型、贴图与材质做 Argon 女性 NPC 外观
[*]沿用原版骨架与共享动画，动作、注视、手持挂点全部正常
[*]不修改任何游戏文件，可随时启用/停用，存档安全
[*]已去除眼透镜壳等只靠着色器出效果的覆盖层，露出正常的眼球
[/list]

[line]

[b][size=4]安装[/size][/b]

[list=1]
[*]下载你需要的那个文件：[b]x4_rose_argon_add_v1.3.zip[/b] 或 [b]x4_rose_argon_replace_v1.2.zip[/b]
[*]解压到游戏根目录下的 [font=Courier New]extensions\[/font] 文件夹，得到：
[code]X4 Foundations\extensions\x4_rose_mod\
    content.xml
    ext_01.cat
    ext_01.dat[/code]
[*]启动游戏 → 主菜单 [b]扩展[/b]（Extensions）→ 勾选 [b]Rose Winters (RE8)[/b]
[*]读取存档或开新档，招募/查看 [b]Argon 女性[/b] 船员
[/list]

[b]提示[/b]：NPC 外观在生成时决定，已在场景里的 NPC 不会立刻变化。
移动到有 Argon 女性的区域（空间站、舰队）或重新招募即可。

[b]提示[/b]：两个包写的文件名相同，换形态是[b]替换文件夹内容[/b]，不是叠加安装。

[line]

[b][size=4]卸载[/size][/b]

删除 [font=Courier New]extensions\x4_rose_mod\[/font] 整个文件夹，或在扩展菜单里取消勾选。
本 mod 不改动任何游戏原文件，卸载后存档可继续使用。

[line]

[b][size=4]「加入外观池」版是怎么做的[/size][/b]

新增一条 macro（继承原版 base macro，种族/性别标记、眼球位置、捏脸系统照常生效），
并往 [b]6 个[/b] Argon 女性外观池各追加一条选择：

[code]argon.civilian.female             6 个原版候选保留
argon.commander.female            3
argon.marine.female               3
argon.pilot.female                3
argon.service.female              3
argon.factiondiplomat.female      1[/code]

[b]没有一个原版 macro 被改动，没有一条池内原有条目被删[/b] —— 所以萝丝只是随机
出现的其中一个，而不是唯一选项。池的清单直接从游戏文件里读出来，DLC 新增的池也会
被带进来；只做路由的派系池（如 Antigone / Hatikvah 外交官池）不需要单独写。

[b]「全部替换」版[/b]则是把这 6 个池整节点换掉（池里没有原版 fallback），
所以每个 Argon 女性都是萝丝，包括不走外观池的剧情/任务 NPC。

[line]

[b][size=4]已知缺陷与限制[/size][/b]

动作绑定的正确性已经过关（四肢不会反向弯折），但[b]这不是一比一还原[/b]。
以下为全部已知问题：

[b]1. 夹克上有几条深色竖条纹[/b]
原版此处应为灰色装饰线。原因是夹克表面有一层「卡片几何」（拉链底座、金属件一类），
其 UV 落在贴图图集的深色区域上。已改善但仍可见。

[b]2. 手和头发是刚体[/b]
[list]
[*][b]手指绑定在手掌上[/b]，不单独做动作 —— 这是为了保住手型：萝丝的手指原本是
并拢的，而 X4 的骨骼是张开的，逐指对齐会把虎口撕开一道口子。
[*][b]头发整体绑定在头部骨上[/b]，不会飘动 —— X4 的 91 根骨架里没有发丝链。
[*][b]脚趾[/b]相对骨骼偏离约 5 cm —— 这是为了让鞋底平整地踩在地面上。
[/list]

[b]3. 手部表面不够光滑[/b]
手指上原本的一圈圈横向环纹已消除，但手背/手心的法线贴图仍不如原版干净。

[b]4. 模型精度低于原版萝丝[/b]
X4 的 NPC 是实例化渲染（空间站里同时几十个），资产顶点数必须贴近原版的
约 5k/资产。本 mod 控制在约 6 倍，代价是面部与衣物比源模型粗糙。
[b]若在空间站出现频闪或地图卡死[/b]，说明显存吃紧，请降低画质设置。

[b]5. 眼球偏灰白[/b]
RE8 用着色器参数渲染虹膜与瞳孔，而 X4 的材质系统只接受
diffuse / normal / smoothness 三张贴图，这一层无法直接搬运。

[b]6. 夹克袖口有深色斑块[/b]
与第 1 点同源，程度较轻。

[b]7. 没有面部表情与口型[/b]
X4 的捏脸系统（facemods）与语音口型对本 mod 无效，萝丝为固定外观、固定表情。

[line]

[b][size=4]兼容性[/size][/b]

[list]
[*]需要 [b]X4: Foundations 9.00[/b] 或更高版本。
[*]不修改任何游戏原文件，可与绝大多数 mod 共存。
[*][b]「加入外观池」版[/b]与其它 Argon 外观替换 mod [b]可以共存[/b]：它只是往同一批
    池里追加一个候选，装得越多，各自分到的出现率越低。
[*][b]「全部替换」版[/b]会覆盖其它 Argon 外观替换 mod 的条目，请单独启用。
[*]存档安全：可随时启用或停用。
[/list]

[b][size=4]前置要求[/size][/b]

需要正版《X4：基石》。本 mod 只包含转换产物，不含任何游戏本体文件。

[line]

[b][size=4]关于制作方式（AI 使用声明）[/size][/b]

本 mod 的[b]工具链[/b]（逆向分析、骨架重定向算法、贴图与打包管线）由一个 AI 助手
协助编写：具体的设计决策、每一轮的实机验证与问题定位由作者完成，AI 负责代码实现
与数据分析。

项目维护着完整的 git 提交历史与逐轮排错日志，可作为开发过程的查证依据。

[b]模型与贴图取自游戏本体并经过格式转换，不是 AI 生成的。[/b]

[line]

[b][size=4]致谢[/size][/b]

[list]
[*][url=https://www.nexusmods.com/x4foundations/mods/2152]X4 Character Converter[/url] by DiCrash / Orion —— 没有这个导出器就没有这一切
[*][b]RE-Mesh-Editor[/b] by Percyqaz 及其贡献者
[*][b]EGOSOFT[/b] 的 X Tools 与 X4 modding 文档
[/list]

[i]（致谢列出的都是制作本 mod 所用的工具，玩家无需额外安装。）[/i]

[b][size=4]版权[/size][/b]

《Resident Evil》为 CAPCOM 商标，《X4：Foundations》为 EGOSOFT 商标。
本 mod 为非官方粉丝作品，仅供个人使用，不含任何游戏本体资产。
萝丝·温特斯的模型与贴图版权归 CAPCOM 所有。
